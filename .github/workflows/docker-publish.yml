name: Publish Docker Images (latest only)

on:
  push:
    branches: [ "main" ] # qualquer commit direto na main
    paths:
      - "implementations/**"
      - ".github/workflows/docker-publish.yml"
  workflow_dispatch: # opcional: disparo manual

permissions:
  contents: read
  packages: write

env:
  DOCKERHUB_ORG: ${{ vars.DOCKERHUB_ORG || secrets.DOCKERHUB_USERNAME }}

concurrency:
  group: docker-publish-main
  cancel-in-progress: true

jobs:
  build-and-push:
    name: Build & Push (${{ matrix.name }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: collector
            context: ingestion-environment/implementations/collector
            dockerfile: ingestion-environment/implementations/collector/Dockerfile
            image: collector

          - name: validator
            context: ingestion-environment/implementations/validator
            dockerfile: ingestion-environment/implementations/validator/Dockerfile
            image: validator

          - name: schema-loader
            context: ingestion-environment/implementations/schema-registry
            dockerfile: ingestion-environment/implementations/schema-registry/Dockerfile
            image: schema-loader

          - name: cred-perm-loader
            context: ingestion-environment/implementations/cred-perm-loader
            dockerfile: ingestion-environment/implementations/cred-perm-loader/Dockerfile
            image: cred-perm-loader
          - name: enricher
            context: ingestion-environment/implementations/enricher
            dockerfile: ingestion-environment/implementations/enricher/Dockerfile
            image: enricher

          - name: real-time-loader
            context: ingestion-environment/implementations/real-time-loader
            dockerfile: ingestion-environment/implementations/real-time-loader/Dockerfile
            image: real-time-loader

          - name: batch-loader
            context: ingestion-environment/implementations/batch-loader
            dockerfile: ingestion-environment/implementations/batch-loader/Dockerfile
            image: batch-loader

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build & Push (multi-arch, tag=latest)
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ env.DOCKERHUB_ORG }}/${{ matrix.image }}:latest
          cache-from: type=registry,ref=${{ env.DOCKERHUB_ORG }}/${{ matrix.image }}:buildcache
          cache-to: type=registry,ref=${{ env.DOCKERHUB_ORG }}/${{ matrix.image }}:buildcache,mode=max
